[package]
name = "core-domain"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Pure, zero-I/O decision-engine and knowledge-citation types for VoteAssist India. Ported from the TypeScript prototype at packages/decision-engine."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
url = "2"
# Dev-only: kb-content is a sibling pure-domain crate (also zero-I/O), used
# here only to verify tree citations resolve to real entries — the same
# cross-crate consistency check the TypeScript prototype's
# knowledge-integration.test.ts performs. core-domain's production code
# never depends on kb-content.
kb-content = { workspace = true }
